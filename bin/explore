#!/usr/bin/env python3

import os
import sys
sys.path.append(os.path.join(os.path.dirname(__file__), '..'))

from prediction import Config
from prediction import Experiment
from prediction import Explorer
from prediction import Input
from prediction import Random
from prediction import Reference
from prediction import support
import argparse
import json

def main(config):
    support.loggalize()
    Random.initialize(config.seed)
    input = Input(config.experiment.input)
    path = config.experiment.output.get('path', support.default_output())
    config.experiment.output.path = os.path.join(path, 'reference')
    reference = Reference(input, config.experiment)
    reference.run_comparison('testing')
    config.experiment.output.path = path
    explorer = Explorer(input, config)
    case, step_count = explorer.run()
    config = explorer.configure(case, restore=step_count)
    experiment = Experiment(input, config)
    experiment.run_comparison('testing')

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('--config', required=True)
    parser.add_argument('--input')
    parser.add_argument('--output')
    arguments = parser.parse_args()
    config = Config(json.loads(open(arguments.config).read()))
    if isinstance(config.experiment, str):
        config.experiment = Config(json.loads(open(config.experiment).read()))
    if arguments.input is not None:
        config.experiment.input.path = arguments.input
    if arguments.output is not None:
        config.experiment.output.path = arguments.output
    main(config)
